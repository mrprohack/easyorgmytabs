{
  "name": "easy-organize-my-tabs",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Privacy-first Chrome and Edge tab organizer.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node tests/validate-extension.mjs",
    "check": "npm test && npm run validate && node --check src/background.js && node --check src/lib/core.js && node --check src/lib/chrome-api.js && node --check src/lib/settings.js && node --check src/popup/popup.js && node --check src/options/options.js"
  },
  "engines": {
    "node": ">=20"
  }
}
